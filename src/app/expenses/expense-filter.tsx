"use client";
import {Search} from "lucide-react"; import {useState} from "react";
export function ExpenseFilter(){const [q,setQ]=useState("");const [month,setMonth]=useState("");const apply=(query:string,selectedMonth:string)=>document.querySelectorAll<HTMLElement>("[data-expense]").forEach(row=>{const text=row.dataset.expense??"";const date=row.dataset.date??"";row.hidden=!(text.includes(query.toLowerCase())&&(!selectedMonth||date.startsWith(selectedMonth)))});return <div className="flex flex-wrap gap-3 border-b p-4"><label className="relative flex-1"><Search size={16} className="absolute left-3 top-2.5 text-slate-400"/><input value={q} onChange={e=>{setQ(e.target.value);apply(e.target.value,month)}} placeholder="ค้นหาหมวดหมู่หรือรายละเอียด" className="w-full rounded-lg border py-2 pl-9 pr-3 text-sm"/></label><input type="month" value={month} onChange={e=>{setMonth(e.target.value);apply(q,e.target.value)}} className="rounded-lg border px-3 py-2 text-sm"/></div>}
