"use client";
import {Pencil,Trash2,X} from "lucide-react"; import {useState,useTransition} from "react"; import {archiveExpense,updateExpense} from "./actions";
type Expense={id:string;expense_date:string;category:string;description:string|null;amount:number};
export function ExpenseActions({expense,canArchive}:{expense:Expense;canArchive:boolean}){const [open,setOpen]=useState(false);const [pending,startTransition]=useTransition();return <><div className="flex gap-1"><button onClick={()=>setOpen(true)} className="rounded p-1.5 text-teal-700 hover:bg-teal-50"><Pencil size={16}/></button>{canArchive&&<button onClick={()=>{if(window.confirm("ปิดใช้งานรายการรายจ่ายนี้?"))startTransition(()=>archiveExpense(expense.id))}} className="rounded p-1.5 text-red-700 hover:bg-red-50"><Trash2 size={16}/></button>}</div>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4" onMouseDown={()=>setOpen(false)}><form action={(fd)=>startTransition(async()=>{await updateExpense(expense.id,{date:String(fd.get("date")),category:String(fd.get("category")),description:String(fd.get("description")),amount:Number(fd.get("amount"))});setOpen(false)})} onMouseDown={e=>e.stopPropagation()} className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between"><h2 className="font-bold">แก้ไขรายจ่าย</h2><button type="button" onClick={()=>setOpen(false)}><X/></button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm">วันที่<input name="date" type="date" required defaultValue={expense.expense_date} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm">หมวดหมู่<input name="category" required defaultValue={expense.category} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm sm:col-span-2">รายละเอียด<textarea name="description" defaultValue={expense.description??""} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm">จำนวนเงิน<input name="amount" type="number" min="0.01" step="0.01" required defaultValue={expense.amount} className="mt-1 w-full rounded border p-2"/></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="rounded border px-4 py-2">ยกเลิก</button><button disabled={pending} className="rounded bg-teal-600 px-4 py-2 text-white">{pending?"กำลังบันทึก...":"บันทึก"}</button></div></form></div>}</>}
