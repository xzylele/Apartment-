"use client";
import {Pencil,X} from "lucide-react"; import {useState,useTransition} from "react"; import {updateLeaseDetails} from "./actions";
type Lease={id:string;start_date:string;end_date:string;rent_amount:number;deposit_amount:number};
export function LeaseActions({lease}:{lease:Lease}){const [open,setOpen]=useState(false);const [pending,startTransition]=useTransition();return <>{<button onClick={()=>setOpen(true)} className="rounded p-1.5 text-teal-700 hover:bg-teal-50"><Pencil size={16}/></button>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4" onMouseDown={()=>setOpen(false)}><form action={(fd)=>startTransition(async()=>{await updateLeaseDetails(lease.id,{startDate:String(fd.get("startDate")),endDate:String(fd.get("endDate")),rentAmount:Number(fd.get("rentAmount")),depositAmount:Number(fd.get("depositAmount"))});setOpen(false)})} onMouseDown={e=>e.stopPropagation()} className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between"><div><h2 className="font-bold">แก้ไขสัญญาเช่า</h2><p className="text-sm text-slate-500">ปรับระยะเวลาและเงื่อนไขการเช่า</p></div><button type="button" onClick={()=>setOpen(false)}><X/></button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm">วันเริ่ม<input name="startDate" type="date" required defaultValue={lease.start_date} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm">วันสิ้นสุด<input name="endDate" type="date" required defaultValue={lease.end_date} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm">ค่าเช่าต่อเดือน<input name="rentAmount" type="number" min="0" required defaultValue={lease.rent_amount} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm">เงินประกัน<input name="depositAmount" type="number" min="0" required defaultValue={lease.deposit_amount} className="mt-1 w-full rounded border p-2"/></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="rounded border px-4 py-2">ยกเลิก</button><button disabled={pending} className="rounded bg-teal-600 px-4 py-2 text-white">{pending?"กำลังบันทึก...":"บันทึก"}</button></div></form></div>}</>}
