"use client";
import {useActionState} from "react"; import Link from "next/link"; import {requestRecovery,type RecoveryState} from "./actions";
export function RecoveryForm(){const [state,action,pending]=useActionState<RecoveryState,FormData>(requestRecovery,{});return <form action={action} className="space-y-4"><label className="block text-sm font-medium">อีเมล<input name="email" type="email" required className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2.5"/></label>{state.error&&<p className="text-sm text-red-700">{state.error}</p>}{state.success&&<p className="text-sm text-teal-700">{state.success}</p>}<button disabled={pending} className="w-full rounded-lg bg-teal-600 py-2.5 font-semibold text-white">{pending?"กำลังส่ง...":"ส่งลิงก์ตั้งรหัสผ่านใหม่"}</button><Link href="/login" className="block text-center text-sm text-teal-700">กลับไปหน้าเข้าสู่ระบบ</Link></form>}
