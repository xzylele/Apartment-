"use client";
import{Fragment,useMemo,useState}from"react";import{BanknoteArrowDown,ChevronDown,FileText,Search,X}from"lucide-react";import Link from"next/link";import{InvoiceActions,type EditableInvoice}from"./invoice-actions";
export type InvoiceItem=EditableInvoice&{tenant_name:string;outstanding:number;stored_status:string};const info:Record<string,{label:string;style:string}>={draft:{label:"ฉบับร่าง",style:"bg-slate-100 text-slate-700"},issued:{label:"รอชำระ",style:"bg-amber-50 text-amber-700"},overdue:{label:"เกินกำหนด",style:"bg-rose-50 text-rose-700"},paid:{label:"ชำระแล้ว",style:"bg-teal-50 text-teal-700"},void:{label:"ยกเลิก",style:"bg-slate-100 text-slate-500"}};const money=(v:number)=>`฿${v.toLocaleString("th-TH",{maximumFractionDigits:2})}`;const date=(v:string)=>new Date(`${v}T00:00:00`).toLocaleDateString("th-TH",{day:"numeric",month:"short",year:"numeric"});
export function InvoiceDirectory({invoices}:{invoices:InvoiceItem[]}){const[q,setQ]=useState("");const[month,setMonth]=useState("");const[status,setStatus]=useState("all");const[open,setOpen]=useState<string|null>(null);const filtered=useMemo(()=>invoices.filter(i=>(`${i.invoice_number} ${i.room_number} ${i.tenant_name}`.toLowerCase().includes(q.toLowerCase()))&&(!month||i.billing_month.startsWith(month))&&(status==="all"||i.status===status)),[invoices,q,month,status]);const active=Boolean(q||month||status!=="all");const reset=()=>{setQ("");setMonth("");setStatus("all")};return <section className="mt-6 overflow-hidden rounded-2xl bg-white"><div className="flex flex-wrap items-center justify-between gap-3 border-b p-5"><div><h2 className="font-bold">รายการใบแจ้งหนี้</h2><p className="text-xs text-slate-500">แสดง {filtered.length} จาก {invoices.length} รายการ · กดเลขบิลเพื่อดูรายละเอียด</p></div><Link href="/payments" className="inline-flex items-center gap-2 rounded-xl bg-teal-600 px-4 py-2 text-sm font-semibold text-white"><BanknoteArrowDown size={16}/>รับชำระเงิน</Link></div><div className="grid gap-3 border-b p-4 sm:grid-cols-2 lg:grid-cols-[1fr_180px_170px_auto]"><label className="relative"><Search size={16} className="absolute left-3 top-3 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ค้นหาเลขบิล ห้อง หรือผู้เช่า" className="w-full rounded-xl border py-2.5 pl-9 pr-3 text-sm"/></label><input type="month" value={month} onChange={e=>setMonth(e.target.value)} className="rounded-xl border px-3 py-2 text-sm"/><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border px-3 py-2 text-sm"><option value="all">ทุกสถานะ</option><option value="issued">รอชำระ</option><option value="overdue">เกินกำหนด</option><option value="paid">ชำระแล้ว</option><option value="void">ยกเลิก</option></select>{active?<button onClick={reset} className="inline-flex items-center justify-center gap-1 rounded-xl border px-3 py-2 text-sm"><X size={15}/>ล้าง</button>:<span/>}</div>{!filtered.length?<div className="p-12 text-center"><FileText className="mx-auto text-slate-300"/><p className="mt-3 font-medium">ไม่พบใบแจ้งหนี้</p></div>:<div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead><tr><th className="px-5 py-3">เลขที่ / รอบบิล</th><th className="px-5 py-3">ห้อง / ผู้เช่า</th><th className="px-5 py-3">ครบกำหนด</th><th className="px-5 py-3 text-right">ยอดรวม</th><th className="px-5 py-3 text-right">คงเหลือ</th><th className="px-5 py-3">สถานะ</th><th className="px-5 py-3 text-right">จัดการ</th></tr></thead><tbody>{filtered.map(i=><Fragment key={i.id}>{<tr className="border-t"><td className="px-5 py-4"><button onClick={()=>setOpen(open===i.id?null:i.id)} className="text-left"><b className="flex items-center gap-1">{i.invoice_number}<ChevronDown size={14} className={open===i.id?"rotate-180":""}/></b><small className="text-slate-500">{i.billing_month.slice(0,7)}</small></button></td><td className="px-5 py-4"><b>ห้อง {i.room_number}</b><small className="block text-slate-500">{i.tenant_name}</small></td><td className={`whitespace-nowrap px-5 py-4 ${i.status==="overdue"?"font-medium text-rose-700":""}`}>{date(i.due_date)}</td><td className="px-5 py-4 text-right font-semibold">{money(i.total_amount)}</td><td className="px-5 py-4 text-right font-bold text-amber-800">{money(i.outstanding)}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${(info[i.status]??info.draft).style}`}>{(info[i.status]??info.draft).label}</span></td><td className="px-5 py-4"><InvoiceActions invoice={i}/></td></tr>}{open===i.id&&<tr key={`${i.id}-detail`} className="bg-slate-50"><td colSpan={7} className="px-5 py-4"><div className="grid gap-3 sm:grid-cols-3 lg:grid-cols-6">{[["ค่าเช่า",i.rent_amount],["ค่าน้ำ",i.water_amount],["ค่าไฟ",i.electric_amount],["อื่น ๆ",i.other_amount],["รับแล้ว",i.paid_amount],["คงเหลือ",i.outstanding]].map(([label,value])=><div key={String(label)}><p className="text-xs text-slate-500">{label}</p><b>{money(Number(value))}</b></div>)}</div></td></tr>}</Fragment>)}</tbody></table></div>}</section>}