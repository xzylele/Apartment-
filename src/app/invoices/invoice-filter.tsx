"use client";
import { Search, X } from "lucide-react";
import { useEffect, useState } from "react";
export function InvoiceFilter(){const[q,setQ]=useState("");const[month,setMonth]=useState("");const[status,setStatus]=useState("all");const[count,setCount]=useState(0);const apply=(query:string,m:string,s:string)=>{let total=0;document.querySelectorAll<HTMLElement>("[data-invoice]").forEach(row=>{const show=(row.dataset.invoice??"").includes(query.toLowerCase())&&(!m||(row.dataset.month??"").startsWith(m))&&(s==="all"||row.dataset.status===s);row.hidden=!show;if(show)total++});setCount(total)};useEffect(()=>setCount(document.querySelectorAll("[data-invoice]").length),[]);const reset=()=>{setQ("");setMonth("");setStatus("all");apply("","","all")};const active=Boolean(q||month||status!=="all");return <div className="border-b border-slate-100 p-4"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-[minmax(240px,1fr)_180px_170px_auto]"><label className="relative"><Search size={16} className="absolute left-3 top-3 text-slate-400"/><input value={q} onChange={e=>{setQ(e.target.value);apply(e.target.value,month,status)}} placeholder="ค้นหาเลขบิล ห้อง หรือชื่อผู้เช่า" className="w-full rounded-xl border py-2.5 pl-9 pr-3 text-sm"/></label><input aria-label="กรองรอบบิล" type="month" value={month} onChange={e=>{setMonth(e.target.value);apply(q,e.target.value,status)}} className="rounded-xl border px-3 py-2 text-sm"/><select aria-label="กรองสถานะ" value={status} onChange={e=>{setStatus(e.target.value);apply(q,month,e.target.value)}} className="rounded-xl border px-3 py-2 text-sm"><option value="all">ทุกสถานะ</option><option value="issued">รอชำระ</option><option value="overdue">เกินกำหนด</option><option value="paid">ชำระแล้ว</option><option value="draft">ฉบับร่าง</option><option value="void">ยกเลิก</option></select>{active?<button type="button" onClick={reset} className="inline-flex items-center justify-center gap-1 rounded-xl border border-slate-200 px-3 py-2 text-sm text-slate-600"><X size={15}/>ล้าง</button>:<span/>}</div><p className="mt-3 text-xs text-slate-500">พบ {count} รายการ</p></div>}