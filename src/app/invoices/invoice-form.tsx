"use client";
import {useActionState,useState} from "react";import{ChevronDown,FilePlus2,Info}from"lucide-react";import{generateInvoices,type InvoiceState}from"./actions";
const initialState:InvoiceState={};const thisMonth=new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Bangkok",year:"numeric",month:"2-digit"}).format(new Date());const dueFor=(month:string,day:number)=>{const[y,m]=month.split("-").map(Number);return `${month}-${String(Math.min(day,new Date(y,m,0).getDate())).padStart(2,"0")}`};
export function InvoiceForm({defaultDueDay}:{defaultDueDay:number}){const[state,action,pending]=useActionState(generateInvoices,initialState);const[open,setOpen]=useState(false);const[month,setMonth]=useState(thisMonth);const[dueDate,setDueDate]=useState(dueFor(thisMonth,defaultDueDay));const field="mt-1 w-full rounded-xl border px-3 py-2.5 text-sm";return <section className="overflow-hidden rounded-2xl bg-white"><button type="button" onClick={()=>setOpen(!open)} className="flex w-full items-center justify-between p-5 text-left"><span className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-teal-50 text-teal-700"><FilePlus2 size={19}/></span><span><b className="block">ออกใบแจ้งหนี้ประจำเดือน</b><small className="text-slate-500">สร้างบิลให้ทุกห้องที่มีสัญญาเช่า</small></span></span><ChevronDown className={open?"rotate-180":""} size={19}/></button>{open&&<form action={action} className="border-t p-5"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-[220px_220px_1fr_auto]"><label className="text-sm font-medium">รอบบิล<input name="month" type="month" required value={month} onChange={e=>{setMonth(e.target.value);setDueDate(dueFor(e.target.value,defaultDueDay))}} className={field}/></label><label className="text-sm font-medium">วันครบกำหนด<input name="dueDate" type="date" required value={dueDate} onChange={e=>setDueDate(e.target.value)} className={field}/></label><p className="flex items-start gap-2 rounded-xl bg-blue-50 p-3 text-xs text-blue-800 lg:self-end"><Info size={15}/>เติมจาก Settings อัตโนมัติและแก้ไขได้</p><button disabled={pending} className="h-11 self-end rounded-xl bg-teal-600 px-5 text-sm font-semibold text-white">{pending?"กำลังออกบิล...":"ออกบิลทุกห้อง"}</button></div>{state.error&&<p className="mt-4 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{state.error}</p>}{state.success&&<p className="mt-4 rounded-xl bg-teal-50 p-3 text-sm text-teal-700">{state.success}</p>}<p className="mt-3 text-xs text-slate-500">ห้องที่มีบิลในเดือนนี้แล้วจะถูกข้ามอัตโนมัติ</p></form>}</section>}