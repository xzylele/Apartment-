"use client";
import {Pencil,X} from "lucide-react"; import {useState,useTransition} from "react"; import {updateMeterReading} from "./actions";
type Reading={id:string;room_id:string;reading_month:string;water_previous:number;water_current:number;electric_previous:number;electric_current:number};
export function MeterActions({reading}:{reading:Reading}){const [open,setOpen]=useState(false);const [pending,startTransition]=useTransition();return <>{<button onClick={()=>setOpen(true)} className="rounded p-1.5 text-teal-700 hover:bg-teal-50"><Pencil size={16}/></button>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4" onMouseDown={()=>setOpen(false)}><form action={(fd)=>startTransition(async()=>{const ok=await updateMeterReading(reading.id,reading.room_id,reading.reading_month,Number(fd.get("waterCurrent")),Number(fd.get("electricCurrent")));if(ok)setOpen(false)})} onMouseDown={e=>e.stopPropagation()} className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between"><div><h2 className="font-bold">แก้ไขเลขมิเตอร์</h2><p className="text-sm text-slate-500">เดือน {reading.reading_month.slice(0,7)}</p></div><button type="button" onClick={()=>setOpen(false)}><X/></button></div><div className="mt-5 grid gap-4"><label className="text-sm">เลขมิเตอร์น้ำ<input name="waterCurrent" type="number" min={reading.water_previous} step="0.01" required defaultValue={reading.water_current} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm">เลขมิเตอร์ไฟ<input name="electricCurrent" type="number" min={reading.electric_previous} step="0.01" required defaultValue={reading.electric_current} className="mt-1 w-full rounded border p-2"/></label></div><p className="mt-3 text-xs text-slate-500">แก้ไขไม่ได้หากมีการออกบิลของเดือนนี้แล้ว</p><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="rounded border px-4 py-2">ยกเลิก</button><button disabled={pending} className="rounded bg-teal-600 px-4 py-2 text-white">{pending?"กำลังบันทึก...":"บันทึก"}</button></div></form></div>}</>}
