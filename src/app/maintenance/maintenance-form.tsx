"use client";
import { useActionState } from "react";
import { Wrench } from "lucide-react";
import { createMaintenance, type MaintenanceState } from "./actions";
const initialState: MaintenanceState = {}; const today = new Date().toISOString().slice(0,10);
export function MaintenanceForm({ rooms }: { rooms: { id: string; room_number: string }[] }) { const [state, action, pending] = useActionState(createMaintenance, initialState); const field = "mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm"; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="flex gap-2 font-semibold"><Wrench size={18} className="text-teal-600"/>แจ้งงานซ่อม</h2><form action={action} className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-sm font-medium">ห้อง<select name="roomId" className={field}>{rooms.map((room) => <option key={room.id} value={room.id}>ห้อง {room.room_number}</option>)}</select></label><label className="text-sm font-medium">ความเร่งด่วน<select name="priority" className={field}><option value="1">สูง</option><option value="2">ปกติ</option><option value="3">ต่ำ</option></select></label><label className="text-sm font-medium">วันนัดดำเนินการ<input name="scheduledDate" type="date" required defaultValue={today} className={field}/></label><label className="text-sm font-medium sm:col-span-2">หัวข้องาน<input name="title" required className={field} placeholder="เช่น เครื่องทำน้ำอุ่นไม่ทำงาน"/></label><label className="text-sm font-medium sm:col-span-2">รายละเอียด<textarea name="description" className={field}/></label><p className="text-xs text-slate-500 sm:col-span-2">เติมวันนัดเป็นวันนี้ให้แล้ว แต่เลือกวันอื่นได้</p><div className="sm:col-span-2">{state.error && <p className="mb-3 text-sm text-red-700">{state.error}</p>}{state.success && <p className="mb-3 text-sm text-teal-700">{state.success}</p>}<button disabled={pending || !rooms.length} className="rounded-lg bg-teal-600 px-4 py-2 text-sm font-semibold text-white disabled:bg-teal-400">{pending ? "กำลังบันทึก..." : "ส่งแจ้งซ่อม"}</button></div></form></section>; }
