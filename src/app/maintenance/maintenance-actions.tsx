"use client";
import {Pencil,X} from "lucide-react"; import {useState,useTransition} from "react"; import {updateMaintenance} from "./actions";
type Request={id:string;title:string;description:string|null;priority:number;status:string;scheduled_date:string};
export function MaintenanceActions({request}:{request:Request}){const [open,setOpen]=useState(false);const [pending,startTransition]=useTransition();return <>{<button onClick={()=>setOpen(true)} className="rounded p-1.5 text-teal-700 hover:bg-teal-50"><Pencil size={16}/></button>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4" onMouseDown={()=>setOpen(false)}><form action={(fd)=>startTransition(async()=>{await updateMaintenance(request.id,{title:String(fd.get("title")),description:String(fd.get("description")),priority:Number(fd.get("priority")),status:String(fd.get("status")),scheduledDate:String(fd.get("scheduledDate"))});setOpen(false)})} onMouseDown={e=>e.stopPropagation()} className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between"><h2 className="font-bold">แก้ไขงานซ่อม</h2><button type="button" onClick={()=>setOpen(false)}><X/></button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm sm:col-span-2">หัวข้อ<input name="title" required defaultValue={request.title} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm sm:col-span-2">รายละเอียด<textarea name="description" defaultValue={request.description??""} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm">วันนัด<input name="scheduledDate" type="date" required defaultValue={request.scheduled_date} className="mt-1 w-full rounded border p-2"/></label><label className="text-sm">ความเร่งด่วน<select name="priority" defaultValue={request.priority} className="mt-1 w-full rounded border p-2"><option value="1">สูง</option><option value="2">ปกติ</option><option value="3">ต่ำ</option></select></label><label className="text-sm">สถานะ<select name="status" defaultValue={request.status} className="mt-1 w-full rounded border p-2"><option value="open">เปิดงาน</option><option value="in_progress">กำลังดำเนินการ</option><option value="completed">เสร็จแล้ว</option><option value="cancelled">ยกเลิก</option></select></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="rounded border px-4 py-2">ยกเลิก</button><button disabled={pending} className="rounded bg-teal-600 px-4 py-2 text-white">{pending?"กำลังบันทึก...":"บันทึก"}</button></div></form></div>}</>}
