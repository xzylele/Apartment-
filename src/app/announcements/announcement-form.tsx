"use client";
import {useActionState} from "react"; import {createAnnouncement,type AnnouncementState} from "./actions";
export function AnnouncementForm(){const [state,action,pending]=useActionState<AnnouncementState,FormData>(createAnnouncement,{});return <form action={action} className="rounded-2xl bg-white p-5 shadow-sm"><div className="grid gap-3"><input name="title" required placeholder="หัวข้อประกาศ" className="rounded-lg border border-slate-300 px-3 py-2"/><textarea name="content" required placeholder="รายละเอียดประกาศ" rows={4} className="rounded-lg border border-slate-300 px-3 py-2"/>{state.error&&<p className="text-sm text-red-700">{state.error}</p>}{state.success&&<p className="text-sm text-teal-700">{state.success}</p>}<button disabled={pending} className="w-fit rounded-lg bg-teal-600 px-4 py-2 text-sm font-semibold text-white">{pending?"กำลังเผยแพร่...":"เผยแพร่ประกาศ"}</button></div></form>}
