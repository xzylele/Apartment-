"use client";
import {useActionState} from "react"; import {reportIssue,type TenantRequestState} from "./actions";
export function IssueForm(){const [state,action,pending]=useActionState<TenantRequestState,FormData>(reportIssue,{});return <form action={action} className="mt-4 space-y-3"><input name="title" required placeholder="หัวข้อปัญหา" className="w-full rounded-lg border border-slate-300 px-3 py-2"/><textarea name="description" placeholder="รายละเอียดเพิ่มเติม" className="w-full rounded-lg border border-slate-300 px-3 py-2" rows={3}/>{state.error&&<p className="text-sm text-red-700">{state.error}</p>}{state.success&&<p className="text-sm text-teal-700">{state.success}</p>}<button disabled={pending} className="rounded-lg bg-teal-600 px-4 py-2 text-sm font-semibold text-white">{pending?"กำลังส่ง...":"ส่งแจ้งซ่อม"}</button></form>}
