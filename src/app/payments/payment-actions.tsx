"use client";

import { useState, useTransition } from "react";
import { ExternalLink, FileImage, Pencil, Upload, X } from "lucide-react";
import { updatePayment } from "./actions";
import { bangkokDateTimeInput } from "@/lib/bangkok-time";

type Payment = { id: string; invoice_number: string; room_number: string; amount: number; paid_at: string; method: string; reference: string | null; slip_path: string | null; slip_url: string | null };
const toLocal = bangkokDateTimeInput;
const field = "mt-1.5 w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm";

export function PaymentActions({ payment }: { payment: Payment }) {
  const [open, setOpen] = useState(false);
  const [pending, startTransition] = useTransition();
  const [error, setError] = useState("");
  const save = (formData: FormData) => startTransition(async () => { const result = await updatePayment(payment.id, formData); if (result.error) setError(result.error); else { setError(""); setOpen(false); } });

  return <><div className="flex items-center justify-end gap-1">{payment.slip_url && <a href={payment.slip_url} target="_blank" rel="noreferrer" title="เปิดหลักฐาน" className="grid h-8 w-8 place-items-center rounded-lg text-blue-700 hover:bg-blue-50"><FileImage size={16}/></a>}<button onClick={() => setOpen(true)} title="แก้ไขรายการ" className="grid h-8 w-8 place-items-center rounded-lg text-teal-700 hover:bg-teal-50"><Pencil size={16}/></button></div>
    {open && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/45 p-4 backdrop-blur-sm" onMouseDown={() => setOpen(false)}><form action={save} role="dialog" aria-modal="true" onMouseDown={(event) => event.stopPropagation()} className="max-h-[92vh] w-full max-w-xl overflow-y-auto rounded-2xl bg-white shadow-2xl"><div className="flex items-start justify-between border-b border-slate-100 px-6 py-4"><div><h2 className="font-bold">แก้ไขรายการรับชำระ</h2><p className="mt-0.5 text-sm text-slate-500">{payment.invoice_number} · ห้อง {payment.room_number}</p></div><button type="button" onClick={() => setOpen(false)} className="grid h-9 w-9 place-items-center rounded-lg bg-slate-100 text-slate-600"><X size={18}/></button></div><div className="grid gap-4 p-6 sm:grid-cols-2"><label className="text-sm font-medium">ยอดรับชำระ<input name="amount" type="number" min="0.01" step="0.01" required defaultValue={payment.amount} className={field}/></label><label className="text-sm font-medium">วันและเวลา<input name="paidAt" type="datetime-local" required defaultValue={toLocal(payment.paid_at)} className={field}/></label><label className="text-sm font-medium">วิธีชำระ<select name="method" defaultValue={payment.method} className={field}><option value="cash">เงินสด</option><option value="transfer">โอนเงิน</option><option value="qr">QR Code</option></select></label><label className="text-sm font-medium">เลขอ้างอิง<input name="reference" defaultValue={payment.reference ?? ""} placeholder="ไม่ระบุ" className={field}/></label><div className="sm:col-span-2"><p className="text-sm font-medium">หลักฐานการชำระ</p>{payment.slip_url ? <div className="mt-2 flex flex-wrap items-center justify-between gap-3 rounded-xl border border-blue-100 bg-blue-50 p-3"><a href={payment.slip_url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm font-medium text-blue-700"><FileImage size={17}/>เปิดหลักฐานปัจจุบัน <ExternalLink size={14}/></a><label className="inline-flex items-center gap-2 text-xs text-rose-700"><input name="removeSlip" type="checkbox" className="min-h-0"/>ลบหลักฐานเดิม</label></div> : <p className="mt-1 text-xs text-slate-500">รายการนี้ยังไม่มีหลักฐาน</p>}<label className="mt-3 flex cursor-pointer items-center gap-3 rounded-xl border border-dashed border-slate-300 bg-slate-50 px-4 py-4 text-sm text-slate-600"><Upload size={18} className="text-teal-600"/><span className="flex-1">{payment.slip_path ? "เลือกไฟล์ใหม่เพื่อแทนที่" : "เลือกสลิปหรือหลักฐาน"}<small className="block text-slate-400">JPG, PNG, WebP หรือ PDF · ไม่เกิน 5 MB</small></span><input name="slip" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" className="max-w-48 min-h-0 border-0 bg-transparent p-0 text-xs"/></label></div>{error && <p className="rounded-xl bg-rose-50 px-4 py-3 text-sm text-rose-700 sm:col-span-2">{error}</p>}</div><div className="flex justify-end gap-3 border-t border-slate-100 px-6 py-4"><button type="button" onClick={() => setOpen(false)} className="rounded-xl border border-slate-200 px-4 py-2 text-sm">ยกเลิก</button><button disabled={pending} className="rounded-xl bg-teal-600 px-5 py-2 text-sm font-semibold text-white">{pending ? "กำลังบันทึก..." : "บันทึกการแก้ไข"}</button></div></form></div>}
  </>;
}